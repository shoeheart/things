<tr>
  <td><%= species.name %></td>
  <td>
    <%= link_to 'Edit', edit_species_path(species), class: "btn btn-primary" %>
    <%= link_to 'Destroy', species, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary" %>
  </td>
</tr>
